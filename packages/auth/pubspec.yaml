name: auth
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:

  cupertino_icons: ^1.0.2

  desktop_webview_auth:
  firebase_auth:
  firebase_auth_desktop: 
  firebase_core:
  firebase_core_desktop: 

  flutter:
    sdk: flutter
  flutter_facebook_auth:
  flutter_signin_button:

  functional_widget_annotation:  

  googleapis: 
  googleapis_auth: 

  hive_flutter:
  hooks_riverpod: 2.0.0-dev.9

  protobuf_for_dart:
    path: ../../../wikibulary/shared/protobufs/protobuf_for_dart
  rewise_storage:
    path: ../rewise_storage 
  wikib_providers:
    path: ../wikib_providers 

dev_dependencies:
  build_runner: 
  functional_widget:

# dependency_overrides:
#   desktop_webview_auth:
#     path: d:/github/flutter_desktop_webview_auth
#   firebase_auth_desktop:
#     path: d:/github/flutterfire_desktop/packages/firebase_auth/firebase_auth_desktop

flutter:
  uses-material-design: true

